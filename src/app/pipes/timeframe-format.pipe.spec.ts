import { TimeframeFormatPipe } from './timeframe-format.pipe';

describe('TimeframeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeframeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
